#!/usr/bin/env bash
# prerm script for diepxuan-repositories
#
# see: dh_installdeb(1)

set -e

case "$1" in
remove | upgrade | deconfigure)
    # Nothing to stop for keyring package
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.
#DEBHELPER#
exit 0
